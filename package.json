{
  "name": "codeship-cache-android-sdk",
  "version": "0.0.1",
  "description": "a simple package that aims to move the already installed android sdk (via terminal) into the node_modules folder just to see if codeship can cache the sdk too",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "bash copy-dependencies.sh"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gion/codeship-cache-android-sdk.git"
  },
  "author": "gion",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/gion/codeship-cache-android-sdk/issues"
  },
  "homepage": "https://github.com/gion/codeship-cache-android-sdk"
}
